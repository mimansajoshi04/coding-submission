class Solution {
    public boolean isPowerOfTwo(int n) {

        if(n<1)
            return false;

        if(n==2 || n==1)
            return true;

        if(n%2==1 && n!=1)
            return false;

        return this.isPowerOfTwo(n/2);
    }
}
