class Solution {
    public int finalValueAfterOperations(String[] operations) {
        int plus = 0;
        int minus = 0;


        for(String s: operations){
            if(s.contains("+"))
                plus++;
            else
                minus++;
        }

        return plus-minus;
    }
}
